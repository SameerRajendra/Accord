# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
!.env.example

# Raw + processed data (large; regenerate via data/ingest_craigslist.py --download).
# NOTE: verify the dataset's license before committing any raw data files.
data/raw/
data/processed/

# Trained model artifacts (large binaries; regenerate via evals/*.py — the
# training/eval code is committed, the artifact itself is not).
models/

# results/ is intentionally committed (reproducible eval outputs) — see results/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
